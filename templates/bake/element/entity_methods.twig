    /**
     * Check if $Identity can add.
     *
     * @param \Authorization\IdentityInterface $Identity
     * @param \{{ classname }} ${{ name }}
     * @return bool
     */
    public function canAdd(IdentityInterface $Identity, {{ name }} ${{ name }}): bool
    {
        return true;
    }

    /**
     * Check if $Identity can edit.
     *
     * @param \Authorization\IdentityInterface $Identity
     * @param \{{ classname }} ${{ name }}
     * @return bool
     */
    public function canEdit(IdentityInterface $Identity, {{ name }} ${{ name }}): bool
    {
        return true;
    }

    /**
     * Check if $Identity can delete.
     *
     * @param \Authorization\IdentityInterface $Identity
     * @param \{{ classname }} ${{ name }}
     * @return bool
     */
    public function canDelete(IdentityInterface $Identity, {{ name }} ${{ name }}): bool
    {
        return true;
    }

    /**
     * Check if $Identity can view.
     *
     * @param \Authorization\IdentityInterface $Identity
     * @param \{{ classname }} ${{ name }}
     * @return bool
     */
    public function canView(IdentityInterface $Identity, {{ name }} ${{ name }}): bool
    {
        return true;
    }
